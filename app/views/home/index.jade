doctype html
html(lang="ja")
	head
		meta(charset="utf-8")
		meta(name="viewport",content="width=device-width,minimum-scale=1,maximum-scale=1")

		link(rel="stylesheet",href="css/materialize.css")
		

		title= page_title

		
			
			
	body
		include ../parts/header

		section.container
			article.row.margin_top
				.col.s12
					img(src="images/sky.jpg",class="responsive-img")

					h1.flow-text Channel

					input(id="form_id", type="text")
					input(id="send",type="submit",class="btn",value="登録")
					#peer_id
					
					.row
						input(id="d_form", type="text")
						input(id="d_call", type="submit")

					h1 My Video
					video(id="video",autoplay,muted)

					h1 Other Video
					video(autoplay, id="other_video")

					audio#audio


				.col.s12
					.row
						.col.s12
							h1.header Play
				
		include ../parts/peer
		script(src="js/peer_listen.js")
